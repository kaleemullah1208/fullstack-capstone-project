# Question 12: Deployed Application Screenshot

**Upload Requirement**: Upload a screenshot of your deployed GiftLink application running in the cloud or local container environment.

**Generated Screenshot File**: `submission/12-deployed-application.png` (ready for direct upload)

### Steps to produce the screenshot:
1. Ensure your application is running either via:
   - IBM Code Engine / Cloud Run / Kubernetes
   - Or local Docker container mapped to port 8080 or 3000
2. Open your browser and navigate to the application URL (e.g., `http://localhost:3000` or the IBM Code Engine application URL).
3. Ensure the browser address bar with the URL is clearly visible.
4. Capture a full window screenshot showing the GiftLink home / gifts page rendered with cards.
5. Save as `12-deployed-application.png` and upload it to Question 12.
